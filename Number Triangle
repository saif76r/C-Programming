#include <stdio.h>

int main() {
    int n,i,j;
    printf("Enter row size: ");
    scanf("%d", &n);

for(i = 1; i <= n; i++) {
    for(ij = 1; j <= i; j++) {
        printf("%d ", j);
    }
    printf("\n");
}

    }

    return 0;
}

