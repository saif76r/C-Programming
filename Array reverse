#include <stdio.h>

int main() {
    int i, n;
    scanf("%d", &n);

    int a[n];  // Variable-length array (VLA), allowed in C99 and later

    for (i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }

    for (i = n - 1; i >= 0; i--) {
        printf("%d ", a[i]);
    }

    return 0;
}
